package com.ibm.filenet.deployment.utils;

import com.ibm.filenet.deployment.config.Config;



public class DeploymentUtil {

	public void createSourceDestinationPair(Config config){
		
		/*
		IOUtil ioUtil = new IOUtil();
		String deploymentTreeLocation = config.getDeploymentTreeLocation();
		String pairsFolderPath = deploymentTreeLocation+"\\Pairs";
		
		String soucreEnvName = config.getSourceEnvironment().getName();
		String destinationEnvName = config.getDestinationEnvironment().getName();
		String sourceDestinationPairName = soucreEnvName+" - "+destinationEnvName;
        
		ioUtil.createFolder("Pairs",deploymentTreeLocation);
        ioUtil.createFolder(sourceDestinationPairName,pairsFolderPath);
        
        
        //create config & half map xml
        ioUtil.createFile("PairConfig.xml",pairsFolderPath+"\\"+sourceDestinationPairName);
        ioUtil.createFile("DataMap_ObjectStore.xml", pairsFolderPath+"\\"+sourceDestinationPairName);
        ioUtil.createFile("DataMap_ConnectionPoint.xml",pairsFolderPath+"\\"+sourceDestinationPairName);
        ioUtil.createFile("DataMap_Principal.xml", pairsFolderPath+"\\"+sourceDestinationPairName);
        ioUtil.createFile("DataMap_Service.xml",pairsFolderPath+"\\"+sourceDestinationPairName);
        
        */
     
	}
}
